# Copy this file to .env and fill in the values.
# Do NOT commit .env (it contains secrets).

# ---------- Required for deploying to Sepolia ----------
# Your wallet's private key (the one that will pay gas). NOT from Etherscan.
# Get it from MetaMask: Account menu → Account details → Export Private Key (or use a new wallet).
DEPLOYER_PRIVATE_KEY=0x your_64_character_hex_here

# RPC URL for Sepolia. If omitted, config uses PublicNode (more stable than rpc.sepolia.org).
# For best reliability use Alchemy/Infura: sign up free, create "Sepolia" app, copy HTTPS URL.
#   Alchemy: https://eth-sepolia.g.alchemy.com/v2/YOUR_KEY
#   Infura: https://sepolia.infura.io/v3/YOUR_KEY
#   Public: https://ethereum-sepolia-rpc.publicnode.com
# SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com

# ---------- Optional: only for contract verification on Etherscan ----------
# From https://etherscan.io/apidashboard — create an API key (free).
# Needed only if you use a verification script; manual verification does not need this.
# ETHERSCAN_API_KEY=YourApiKeyToken

# ---------- Optional: deployment overrides ----------
# Use existing USDC on Sepolia instead of deploying MockERC20 (e.g. Circle's):
# USDC_ADDRESS=0xE1262c4856656d67c9c9cf0c6Acf12df5EfAB4AA
# TREASURY_ADDRESS=0x...
# RESULT_SIGNER_ADDRESS=0x...
