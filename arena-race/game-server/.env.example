# Game server — copy to .env and adjust.
# Do NOT commit .env.

# Server
PORT=3000

# Chain and contract (for entry check and result submission)
CHAIN_RPC_URL=http://127.0.0.1:8545
ESCROW_ADDRESS=

# Result signer (POST /sign, GET /whoami). Run: npm run signer (from arena-race, port 3344)
SIGNER_URL=http://127.0.0.1:3344

# Submitter key (for submitResult tx). Use deployer key for local; do not commit.
# SUBMITTER_PRIVATE_KEY=0x...

# SQLite DB (default: ./data/arena.db)
# DB_PATH=./data/arena.db
